name: yurt_yonetim
description: "Öğrenci Yurdu Kayıt ve Oda Kapasite Yönetimi - Enes Işık"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  cloud_firestore: ^5.6.6

  # State Management
  provider: ^6.1.2

  # Navigation
  go_router: ^14.8.1

  # UI
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
